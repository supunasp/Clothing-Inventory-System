/**
 * Application-wide constants.
 *
 * Only values used in multiple files belong here. Single-use values
 * stay co-located with their component.
 */

// Role names (must match backend/constants.js ROLE_* values)
export const ROLE_ADMIN = 'admin';
export const ROLE_STAFF = 'staff';

// Pagination size for full tables (products, audits, users, admin product list)
export const PAGE_SIZE = 10;
